---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart pgadmin
  ansible.builtin.systemd:
    name: '{{ pgadmin_service_name }}'
    state: restarted
  become: true
